export type Task = { id:number; title:string; description:string; due_date:string|null; category:string; required_action:string; estimated_effort_minutes:number; priority_score:number; priority_level:string; priority_reasoning:string[]; status:string; suggested_owner?:string|null; created_at:string };
export type Dashboard = { total_tasks:number; completed_tasks:number; pending_tasks:number; overdue_tasks:number; completion_rate:number; average_resolution_hours:number; weekly_productivity_trend:{date:string;completed:number}[]; today_focus:Task[]; workload_minutes:number; aging_buckets:Record<string,number> };
